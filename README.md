# OptiTempWorker
Temporary Windows/WSL rollout workers for Opti-Trainer. Automatically installs WSL and Tailscale, uses available CPU and RAM, securely connects to the training host, updates itself, drains active work, and completely removes itself after a chosen time.
